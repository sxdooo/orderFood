// API base URL for WeChat mini program requests.
// - DevTools simulator: 127.0.0.1 works because it runs on the same machine.
// - Real device / preview: MUST use LAN IP of the dev machine.
//   Enable in DevTools: 详情 → 本地设置 → 不校验合法域名 + 不校验HTTPS证书
export const API_BASE_URL = 'http://192.168.1.9:8080/api/v1'
